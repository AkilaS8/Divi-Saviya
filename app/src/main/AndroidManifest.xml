<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edith.divisaviyafinalproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiviSaviyaFinalProject">
        <activity android:name=".Activities.SelectLanguage"></activity>
        <activity android:name=".Activities.UserGuide" />
        <activity android:name=".Activities.LanguageChange" />
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Help" />
        <activity android:name=".Activities.ArticleViewer" />
        <activity android:name=".Activities.Guide" />
        <activity android:name=".Activities.Categories" />
        <activity android:name=".Activities.MyRequestDetails" />
        <activity android:name=".Activities.RequestDetails" />
        <activity android:name=".Activities.AddRequest" />
        <activity android:name=".Activities.RequestMain" />
        <activity android:name=".Activities.ServiceDetails" />
        <activity android:name=".Activities.ProductDetails" />
        <activity android:name=".Activities.MyServiceDetails" />
        <activity android:name=".Activities.MyProductDetails" />
        <activity android:name=".Activities.AddService" />
        <activity android:name=".Activities.AddProduct" />
        <activity android:name=".Activities.MyShop" />
        <activity android:name=".Activities.ForgotPassword" />
        <activity android:name=".Activities.Login" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Activities.UserRegistration" />
        <activity android:name=".Activities.MainMenu" />
        <activity android:name=".Activities.Profile" />
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>